name: Deploy

# CD（脚手架）：推 main 或手动触发时，构建 API 镜像推送 GHCR，SSH 到服务器 docker compose 更新。
# 激活前提：仓库配置 secrets —— DEPLOY_HOST / DEPLOY_USER / DEPLOY_SSH_KEY；
# 服务器已有 /opt/yiren-jianghu/docker-compose.prod.yml 与 .env。详见 deploy/README.md。
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "services/**"
      - "packages/**"
      - "docker-compose*.yml"
      - ".github/workflows/deploy.yml"

concurrency:
  group: deploy-main
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push API image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: services/api/Dockerfile
          push: true
          tags: ghcr.io/${{ github.repository }}/api:main
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Upload deploy assets
        env:
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
        if: env.DEPLOY_HOST != ''
        uses: appleboy/scp-action@v1
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          source: "deploy/scripts/deploy-release.sh,docker-compose.prod.yml,.env.example"
          target: /opt/yiren-jianghu

      - name: Run deploy (script file, auditable)
        env:
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
        if: env.DEPLOY_HOST != ''
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          script: |
            set -e
            cd /opt/yiren-jianghu
            chmod +x deploy/scripts/deploy-release.sh
            ./deploy/scripts/deploy-release.sh main
