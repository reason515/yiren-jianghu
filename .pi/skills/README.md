# `.pi/skills` 已迁移

本目录下的 skill **权威副本**位于：

```text
.cursor/skills/<skill-name>/SKILL.md
```

Cursor 规则入口：`.cursor/rules/`（含始终生效的 `yjh-agents.mdc`）。项目级触发表亦见 `.pi/agent/AGENTS.md`（路径已指向 `.cursor/skills/`）。

各子目录中的 `SKILL.md` 仅为兼容 stub：触发后请 **read `.cursor/skills/...` 权威文件**，勿在 stub 上编辑。修改 skill 只改 `.cursor/skills/`。
